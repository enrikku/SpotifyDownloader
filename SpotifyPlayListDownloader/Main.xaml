<Window
    x:Class="SpotifyDownloader.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Spotify Downloader"
    Width="860"
    Height="480"
    Background="#121212"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <!--  Layout  -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border
            Grid.Row="0"
            Padding="18"
            Background="{StaticResource CardBrush}"
            BorderBrush="#22FFFFFF"
            BorderThickness="0,0,0,1"
            Style="{StaticResource Card}">
            <Grid>
                <TextBlock Style="{StaticResource TitleText}" Text="Spotify Downloader" />
                <Border
                    Height="4"
                    VerticalAlignment="Bottom"
                    Background="{StaticResource AccentBrush}"
                    CornerRadius="2"
                    Opacity="0.9" />
            </Grid>
        </Border>

        <!--  Center Card  -->
        <Grid Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border
                    Grid.Row="1"
                    Margin="24"
                    Padding="24"
                    Background="{StaticResource CardBrush}"
                    CornerRadius="14"
                    Style="{StaticResource Card}">

                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,16"
                            Style="{DynamicResource SubtitleText}"
                            Text="Bienvenido! Elija una de las siguientes opciones:" />

                        <UniformGrid
                            Margin="0,8,0,0"
                            Columns="2"
                            Rows="2">
                            <Button
                                x:Name="btnDownloadPlayList"
                                Margin="6"
                                Click="btnDownloadPlayList_Click"
                                Style="{StaticResource PrimaryButton}"
                                ToolTip="Desacargar todas las canciones de una playlist">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,8,0"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="16"
                                        Text="" />
                                    <TextBlock Text="Descargar Playlist" />
                                </StackPanel>
                            </Button>

                            <Button
                                x:Name="btnDownloadArtist"
                                Margin="6"
                                Click="btnDownloadArtist_Click"
                                Style="{StaticResource PrimaryButton}"
                                ToolTip="Descargar albums, EP; apariciones por artista">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,8,0"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="16"
                                        Text="" />
                                    <TextBlock Text="Descargar Artista" />
                                </StackPanel>
                            </Button>

                            <Button
                                x:Name="btnOpenLogs"
                                Margin="6"
                                Click="btnOpenLogs_Click"
                                Style="{StaticResource PrimaryButton}"
                                ToolTip="Abrir carpeta de logs">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Image
                                        Width="25"
                                        Height="auto"
                                        Margin="0,0,8,0"
                                        Source="/Images/log.png" />
                                    <TextBlock Text="Abrir Logs" />
                                </StackPanel>
                            </Button>
                            <Button
                                x:Name="btnDeleteCache"
                                Margin="6"
                                Click="btnDeleteCache_Click"
                                Style="{StaticResource PrimaryButton}"
                                ToolTip="Eliminar cache">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Image
                                        Width="25"
                                        Height="auto"
                                        Margin="0,0,8,0"
                                        Source="/Images/trash.png" />
                                    <TextBlock Text="Eliminar Cache" />
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>